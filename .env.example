SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=

# Production only (ignored while DEBUG=True). HSTS max-age in seconds; start
# with a small value when rolling out, then raise. Default: 31536000 (1 year).
SECURE_HSTS_SECONDS=31536000
